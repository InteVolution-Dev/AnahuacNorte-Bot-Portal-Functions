function buildFunctionToolFromOpenAPI(openApi, path, method){
    // 1. Extraer metadata
    // 2. Generar "name"
    // 3. Generar "description"
    // 4. Convertir el requestBody.schema → parameters
    // 5. Armar serviceProperties
    // 6. Retornar { functionTool, serviceProperties }
    return;
}

module.exports = {
    buildFunctionToolFromOpenAPI,
};